{
  "name": "ecollectivites",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "generate": "turbo run generate",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "format": "turbo run format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.6.2"
  }
}
